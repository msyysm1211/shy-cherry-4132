export default function About() {
  return (
    <main style={{ padding: '2rem' }}>
      <h1>About (SSG)</h1>
      <p>Purely static page — tests pre-render cache upload via CLI.</p>
    </main>
  );
}
